apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    svc: postgres-service
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: postgres