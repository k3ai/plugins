api: k3ai.in/v1alpha1
kind: Application
metadata:
  name: dummy
  desc: "This is a short description of the dummy plugin. It's long enough to require trim the message but is not too much long anyway...blah,blah,blah"
  tag: "machine-learning"
  version: v0.0.0
  type: kustomize
resources:
# Pre Requisites
- ../common/cert-manager/
- ../common/istio/
# Application logic
- "http://a.b.c"
- "http://d.e.f"
# Post Requisites
- overlays/ingress.yaml
- overlays/rbac.yaml

